# Pinned to the versions used for the reported results (Python 3.9). The exact
# figures in README/MODEL_CARD/outputs are for this tested stack.
pandas==2.3.1
numpy==2.0.2
scikit-learn==1.6.1
scipy==1.13.1
matplotlib==3.9.4
shap==0.49.1
xlrd==2.0.2
anthropic  # optional, unpinned — only needed to generate reason codes via the LLM
